{
  "name": "aura-capilar",
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "setup": "npm install && npm run db:reset -w api",
    "dev": "concurrently -n api,web -c magenta,cyan \"npm run dev -w api\" \"npm run dev -w web\"",
    "dev:api": "npm run dev -w api",
    "dev:web": "npm run dev -w web",
    "db:reset": "npm run db:reset -w api",
    "db:studio": "npm run db:studio -w api"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
